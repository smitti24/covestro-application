import * as productsMocks from '../support/mocks/productMocks';
import * as loginMocks from '../support/mocks/loginUserMocks';

describe('Product Page (In progress)', () => {

  beforeEach(() => {
    // cy.server();
    // loginMocks.loginUser();

    // cy.wait('@loginUser');

    // cy.fixture('products.json').as('allProducts');
    // cy.route('GET', 'http://localhost:3000/products', '@allProducts').as('getAllProducts')

    // cy.fixture('products.json').then(res => {
    //   productsMocks.getProducts(res.payload);
    // });

    // cy.wait('@getAllProducts');
  })

  it('should display a list of products', () => {

    // cy.fixture('products/products.json').as('allProducts');
    // cy.server();
    // cy.request('GET', 'api/products', '@allProducts').as('getAllProducts')

    // cy.visit('/')

    // cy.fixture('products.json').as("productsJSON");

    // cy.server();
    // cy.route('/api/products', "@productsJSON").as("products");
    // cy.wait('@getAllProducts').then((xhr) => {
    //   console.log(xhr)
    //   });

    // cy.get('.product-card').should('have.length', 3);
  })
})
